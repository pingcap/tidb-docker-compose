# Default values for pd0.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: pingcap/tidb-vision
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8010
